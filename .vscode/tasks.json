{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-compose-up-debug",
            "type": "shell",
            "command": "docker compose -f docker-compose.yml -f docker-compose.debug.yml up --build -d && echo 'Waiting for containers...' && sleep 15 && echo 'Containers ready'",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        },
        {
            "label": "docker-compose-down",
            "type": "shell",
            "command": "docker compose -f docker-compose.yml -f docker-compose.debug.yml down",
            "problemMatcher": [],
            "presentation": {
                "reveal": "silent",
                "panel": "dedicated"
            }
        }
    ]
}